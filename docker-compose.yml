version: '3.8'

services:
  api-node:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sahai-api-node
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - ../../ENV/.env
    environment:
      - NODE_ENV=development
